Admission Checker Notebook

How to run (PowerShell):

Install dependencies:

python -m pip install -r requirements.txt

Open the notebook `Admission_Checker.ipynb` in Jupyter or run it using `jupyter notebook`.

The notebook will generate sample data in `data/` and output results to `output/admission_results.xlsx`.
